<div class="btn-group" data-modal-delete-page-target>
    <button class="btn btn-icon btn-sm" type="button" data-bs-toggle="dropdown" data-bs-popper-config='{"strategy":"fixed"}' {{ sylius_test_html_attribute('actions') }}>
        <div><svg xmlns="https://www.w3.org/2000/svg" viewBox="0 0 24 24" class="icon" aria-hidden="true"><path fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 12a1 1 0 1 0 2 0a1 1 0 1 0-2 0m7 0a1 1 0 1 0 2 0a1 1 0 1 0-2 0m7 0a1 1 0 1 0 2 0a1 1 0 1 0-2 0"></path></svg></div>
        {# {{ ux_icon('tabler:dots') }} #}
        {# there is actually an issue with infinite-tree when using an svg to be the dropdown button,
            because the el.className doesn't exists on an svg element.
        #}
    </button>
    <ul class="dropdown-menu">
        {% hook 'dropdown' %}
    </ul>
</div>

{% trans_default_domain 'SyliusHappyCMSPlugin' %}

{% set isModal = (modal is defined and modal) ? true : false %}
{% set isBrowser = (browser is defined and browser) ? true : false %}

{# happy_cms.media.base_url #}
{% set options = {
    'baseUrl' : happy_cms.media.base_url,
    'hideFilesExt' : happy_cms.media.hide_files_ext,
    'mimeTypes' : happy_cms.media.extended_mimes,
    'broadcasting' : happy_cms.media.enable_broadcasting,
    'generatingAlts' : happy_cms.media.enable_generating_alts,
    'locales' : happy_cms.media.locales,
} %}

{% set routes = {
    'files' : path('media.get_files'),
    'upload' : path('media.upload'),
} %}

{% set translations = {
    'add_to_list' : 'happy_cms.media.add.list'|trans,
    'added' : 'happy_cms.media.add.added'|trans,
    'already_exists' : 'happy_cms.media.error.already_exists'|trans,
    'application' : 'happy_cms.media.application'|trans,
    'pdf' : 'happy_cms.media.filter.type.pdf'|trans,
    'oembed' : 'happy_cms.media.filter.type.oembed'|trans,
    'audio' : 'happy_cms.media.audio.main'|trans,
    'bm_add_to_list' : 'happy_cms.media.bookmarks.add'|trans,
    'bm' : 'happy_cms.media.bookmarks.main'|trans,
    'clear' : 'happy_cms.media.clear'|trans({'attr' : 'happy_cms.media.crop.selection'|trans}),
    'copy_success' : 'happy_cms.media.copy.success'|trans,
    'copy' : 'happy_cms.media.copy.main'|trans,
    'create_folder_notif' : 'happy_cms.media.new.create_folder_notif'|trans,
    'create_success' : 'happy_cms.media.create_success'|trans,
    'crop_apply' : 'happy_cms.media.crop.apply'|trans,
    'crop_flip_horizontal' : 'happy_cms.media.crop.flip_horizontal'|trans,
    'crop_flip_vertical' : 'happy_cms.media.crop.flip_vertical'|trans,
    'crop_reset_filters' : 'happy_cms.media.crop.reset_filters'|trans,
    'crop_reset' : 'happy_cms.media.crop.reset'|trans,
    'crop_cancel' : 'happy_cms.media.crop.cancel'|trans,
    'crop_rotate_left' : 'happy_cms.media.crop.rotate_left'|trans,
    'crop_rotate_right' : 'happy_cms.media.crop.rotate_right'|trans,
    'crop_zoom_in' : 'happy_cms.media.crop.zoom_in'|trans,
    'crop_zoom_out' : 'happy_cms.media.crop.zoom_out'|trans,
    'crop' : 'happy_cms.media.crop.main'|trans,
    'delete_success' : 'happy_cms.media.delete.success'|trans,
    'delete' : 'happy_cms.media.delete.main'|trans,
    'description' : 'happy_cms.media.description'|trans,
    'diff' : 'happy_cms.media.editor.diff'|trans,
    'dimension' : 'happy_cms.media.dimension'|trans,
    'downloaded' : 'happy_cms.media.download.downloaded'|trans,
    'editor' : 'happy_cms.media.editor.main'|trans,
    'error_altered_fwli' : 'happy_cms.media.error.altered_fwli'|trans,
    'filter_by' : 'happy_cms.media.filter.by'|trans({'attr' : 'happy_cms.media.'}),
    'filtration' : 'happy_cms.media.filter.filtration'|trans,
    'find' : 'happy_cms.media.find'|trans,
    'focals' : 'happy_cms.media.focals'|trans,
    'folder' : 'happy_cms.media.folder'|trans,
    'found' : 'happy_cms.media.found'|trans,
    'glbl_search_avail' : 'happy_cms.media.search.glbl_avail'|trans,
    'glbl_search' : 'happy_cms.media.search.glbl'|trans,
    'go_to_folder' : 'happy_cms.media.go_to_folder'|trans,
    'image' : 'happy_cms.media.image'|trans,
    'last_modified' : 'happy_cms.media.last_modified'|trans,
    'locked' : 'happy_cms.media.locked'|trans,
    'move_clear' : 'happy_cms.media.move.clear_list'|trans,
    'move_success' : 'happy_cms.media.move.success'|trans,
    'move' : 'happy_cms.media.move.main'|trans,
    'name' : 'happy_cms.media.name'|trans,
    'new_uploads_notif' : 'happy_cms.media.upload.new_uploads_notif'|trans,
    'no_val' : 'happy_cms.media.no_val'|trans,
    'non' : 'happy_cms.media.non'|trans,
    'nothing_found' : 'happy_cms.media.nothing_found'|trans,
    'open' : 'happy_cms.media.open'|trans,
    'close' : 'happy_cms.media.close'|trans,
    'options' : 'happy_cms.media.options'|trans,
    'presets' : 'happy_cms.media.crop.presets'|trans,
    'refresh_notif' : 'happy_cms.media.refresh_notif'|trans,
    'rename_success' : 'happy_cms.media.rename.success'|trans,
    'rename' : 'happy_cms.media.rename.main'|trans,
    'reset' : 'happy_cms.media.move.reset'|trans,
    'save_success' : 'happy_cms.media.save.success'|trans,
    'save' : 'happy_cms.media.save.main'|trans,
    'selected' : 'happy_cms.media.select.selected'|trans,
    'sep_download' : 'happy_cms.media.download.sep'|trans,
    'size' : 'happy_cms.media.size'|trans,
    'sort_by' : 'happy_cms.media.sort_by'|trans,
    'stand_by' : 'happy_cms.media.stand_by'|trans,
    'text' : 'happy_cms.media.text'|trans,
    'to_cp' : 'happy_cms.media.copy.to_cp'|trans,
    'upload_in_progress' : 'happy_cms.media.upload.in_progress'|trans,
    'upload_success' : 'happy_cms.media.upload.success'|trans,
    'video' : 'happy_cms.media.video'|trans,
    'clarity' : 'happy_cms.media.editor.clarity'|trans,
    'pinhole' : 'happy_cms.media.editor.pinhole'|trans,
    'love' : 'happy_cms.media.editor.love'|trans,
    'jarques' : 'happy_cms.media.editor.jarques'|trans,
    'orange_peel' : 'happy_cms.media.editor.orange_peel'|trans,
    'sin_city' : 'happy_cms.media.editor.sin_city'|trans,
    'grungy' : 'happy_cms.media.editor.grungy'|trans,
    'old_boot' : 'happy_cms.media.editor.old_boot'|trans,
    'lomo' : 'happy_cms.media.editor.lomo'|trans,
    'vintage' : 'happy_cms.media.editor.vintage'|trans,
    'cross_process' : 'happy_cms.media.editor.cross_process'|trans,
    'concentrate' : 'happy_cms.media.editor.concentrate'|trans,
    'glowing_sun' : 'happy_cms.media.editor.glowing_sun'|trans,
    'sunrise' : 'happy_cms.media.editor.sunrise'|trans,
    'nostalgia' : 'happy_cms.media.editor.nostalgia'|trans,
    'hemingway' : 'happy_cms.media.editor.hemingway'|trans,
    'her_majesty' : 'happy_cms.media.editor.her_majesty'|trans,
    'hazy_days' : 'happy_cms.media.editor.hazy_days'|trans,
    'brightness' : 'happy_cms.media.editor.brightness'|trans,
    'contrast' : 'happy_cms.media.editor.contrast'|trans,
    'saturation' : 'happy_cms.media.editor.saturation'|trans,
    'vibrance' : 'happy_cms.media.editor.vibrance'|trans,
    'exposure' : 'happy_cms.media.editor.exposure'|trans,
    'hue' : 'happy_cms.media.editor.hue'|trans,
    'sepia' : 'happy_cms.media.editor.sepia'|trans,
    'gamma' : 'happy_cms.media.editor.gamma'|trans,
    'noise' : 'happy_cms.media.editor.noise'|trans,
    'clip' : 'happy_cms.media.editor.clip'|trans,
    'sharpen' : 'happy_cms.media.editor.sharpen'|trans,
    'stack_blur' : 'happy_cms.media.editor.stack_blur'|trans,
    'greyscale' : 'happy_cms.media.editor.greyscale'|trans,
    'invert' : 'happy_cms.media.editor.invert'|trans,
    'public' : 'happy_cms.media.visibility.public'|trans,
    'private' : 'happy_cms.media.visibility.private'|trans,
    'seo.title' : 'happy_cms.media.seo.title'|trans,
    'seo.alt' : 'happy_cms.media.seo.alt'|trans,
    'seo.description' : 'happy_cms.media.seo.description'|trans,
    'edit_metas_success' : 'happy_cms.media.seo.edit_metas'|trans,
    'generate_alt' : 'happy_cms.media.alt.generate_alt'|trans,
    'generate_alt_success' : 'happy_cms.media.alt.generate_alt_success'|trans,
    'generate_alt_no_change' : 'happy_cms.media.alt.generate_alt_no_change'|trans,
    'generate_all_alt' : 'happy_cms.media.alt.generate_alt' ,
    'languages' : 'happy_cms.media.languages' ,
} %}

<media-manager inline-template
               v-cloak
               class="hide-native-scrollbar"
               :config="{{ options|json_encode }}"
               :routes="{{ routes|json_encode }}"
               :translations="{{ translations|json_encode }}"
               :in-modal="{{ modal|default(null) ? 'true' : 'false' }}"
               :hide-ext="{{ hideExt|default(null) ? hideExt|json_encode : '[]' }}"
               :hide-path="{{ hidePath|default(null) ? hidePath|json_encode : '[]' }}"
               :restrict="{{ restrict|default(null) ? restrict|json_encode : '{}' }}"
               :user-id="{{ happy_cms.media.enable_broadcasting ? app.user.id : 0 }}"
               :upload-panel-img-list="{{ patterns|default(null) ?: '[]' }}"
               :enable-editor="{{ (editor is defined and not editor) ? 'false' : 'true' }}"
               :enable-upload="{{ (upload is defined and not upload) ? 'false' : 'true' }}"
               :enable-move="{{ (move is defined and not move) ? 'false' : 'true' }}"
               :enable-rename="{{ (rename is defined and not rename) ? 'false' : 'true' }}"
               :enable-metas="{{ (metas is defined and not metas) ? 'false' : 'true' }}"
               :enable-delete="{{ (delete is defined and not delete) ? 'false' : 'true' }}"
               :enable-bulk-selection="{{ (bulk_selection is defined and not bulk_selection) ? 'false' : 'true' }}"
               :enable-generating-alts="{{ happy_cms.media.enable_generating_alts ? 'true' : 'false' }}"
>

    <div class="media-manager" >

        {# top toolbar #}
        {% include '@SyliusHappyCMSPlugin/media/partials/manager/toolbar.html.twig' %}
        {# ====================================================================== #}

        {# dropzone #}
        {% include '@SyliusHappyCMSPlugin/media/partials/manager/dropzone.html.twig' %}
        {# ====================================================================== #}


        <!--<div class="level-right">
            {# upload preview info #}
            <div class="level-item" v-if="waitingForUpload">
                <nav class="breadcrumb">
                    <ul>
                        <li><p class="level has-text-weight-bold">{% verbatim %}{{ uploadPreviewList.length }}{% endverbatim %} File's</p></li>
                        <li><p class="level has-text-weight-bold">{% verbatim %}{{ uploadPreviewListSize }}{% endverbatim %}</p></li>
                    </ul>
                </nav>
            </div>

            {# toggle sidebar #}
            <div class="level-item" v-show="!isLoading && !waitingForUpload">
                {# show/hide toolbar #}
                <div class="block sm:hidden">
                    <button type="button" class="button is-link __stack-left-toolbarToggle" @click.stop="toolBar = !toolBar">
                        <span class="icon"><icon :name="toolBar ? 'times' : 'bars'"></icon></span>
                    </button>
                </div>
            </div>
        </div>-->

        <div class="media-manager__stack">
            {# path toolbar #}
            {% include '@SyliusHappyCMSPlugin/media/partials/manager/breadcrumb.html.twig' %}
            {# ====================================================================== #}

            <section class="__stack-container">
                {# upload preview #}
                {% include '@SyliusHappyCMSPlugin/media/partials/manager/upload-preview.html.twig' %}

                {# loadings #}
                {% include '@SyliusHappyCMSPlugin/media/partials/manager/loadings.html.twig' %}

                {# overlay #}
                <container-click-overlay></container-click-overlay>

                <div class="extra-func-btns" v-if="enableMove">
                    {# intro #}
{#                    <usage-intro-btn v-show="!isLoading && !waitingForUpload"></usage-intro-btn>#}

                    {# movable list #}
                    <v-touch v-if="allItemsCount && !isLoading && !waitingForUpload && !inMovableList()"
                             class="movable-list"
                             v-tippy="{arrow: true, hideOnClick: false}"
                             :title="'{{ 'add.list'|trans }} (c / x)'"
                             @tap="addToMovableList()">
                        <span class="icon is-large">
                            <icon name="plus" scale="1.2"></icon>
                        </span>
                    </v-touch>

                    <v-touch v-if="allItemsCount && movableItemsCount && !isLoading && !waitingForUpload"
                             class="movable-list mt-4"
                             v-tippy="{arrow: true, hideOnClick: false}"
                             :title="'{{ 'show.list'|trans }}'"
                             @tap="showMovableList()"
                             @hold="clearMovableList()">
                        <span class="icon is-large">
                            <icon name="list-ul" scale="1.2"></icon>
                        </span>
                        <span class="counter">{% verbatim %}{{ movableItemsCount || null }}{% endverbatim %}</span>
                    </v-touch>
                </div>

                {# ====================================================================== #}


                {# files box #}
                <v-touch class="__stack-files mm-animated"
                         :class="{'__stack-sidebar-hidden' : !infoSidebar}"
                         ref="__stack-files"
                         @swiperight="goToPrevFolder()"
                         @swipeleft="goToPrevFolder()"
                         @hold="containerClick($event)"
                         @dbltap="containerClick($event)"
                         @pinchin="containerClick($event)">


                    {# no search #}
                    <section>
                        <div id="no_search" v-show="no_search">
                            <div id="no_search_anim" data-json="/bundles/syliushappycmsplugin/dist/lottie/ice_cream.json"></div>
                            <h3>{{ 'nothing_found'|trans }}</h3>
                        </div>
                    </section>

                    {# files #}
                    <ul class="__files-boxs" ref="filesList">
                        <li v-for="(file, index) in orderBy(allFiles, sortName, sortDirection)"
                            :key="file.name"
                            :data-file-index="index"
                            @click.stop="setSelected(file, index, $event)">
                            <v-touch class="__file-box mm-animated"
                                     :class="{'bulk-selected': IsInBulkList(file), 'selected' : isSelected(file)}"
                                     @swipeup="swipGesture($event, file, index)"
                                     @swipedown="swipGesture($event, file, index)"
                                     @swiperight="swipGesture($event, file, index)"
                                     @swipeleft="swipGesture($event, file, index)"
                                     @hold="pressGesture($event, file, index)"
                                     @dbltap="pressGesture($event, file, index)">

                                {# copy file link #}
                                <div v-if="!fileTypeIs(file, 'folder') && !fileTypeIs(file, 'oembed')"
                                     class="__box-copy-link icon"
                                     @click.stop="copyLink(file.path)"
                                     :title="linkCopied ? trans('copied') : trans('to_cp')"
                                     v-tippy="{arrow: true, hideOnClick: false}"
                                     @hidden="linkCopied = false">
                                    <icon name="regular/clone" scale="0.9"></icon>
                                </div>

                                <div class="__box-data">
                                    <div class="__box-preview">
                                        <template v-if="fileTypeIs(file, 'image')">
                                            <image-intersect
                                                    :file="file"
                                                    :check-for-dimensions="checkForDimensions"
                                                    :browser-support="browserSupport"
                                                    root-el=".__stack-files">
                                            </image-intersect>
                                        </template>

                                        <template v-else-if="fileTypeIs(file, 'oembed')">
                                            <img v-if="file.metas.icon" :src="file.metas.icon">
                                            <icon v-else name="external-link-alt" scale="2.6"></icon>
                                        </template>

                                        <span v-else class="icon is-large">
                                            <icon v-if="fileTypeIs(file, 'folder')" name="folder" scale="2.6"></icon>
                                            <icon v-else-if="fileTypeIs(file, 'application')" name="cogs" scale="2.6"></icon>
                                            <icon v-else-if="fileTypeIs(file, 'compressed')" name="regular/file-archive" scale="2.6"></icon>
                                            <icon v-else-if="fileTypeIs(file, 'video')" name="film" scale="2.6"></icon>
                                            <icon v-else-if="fileTypeIs(file, 'audio')" name="music" scale="2.6"></icon>
                                            <icon v-else-if="fileTypeIs(file, 'pdf')" name="regular/file-pdf" scale="2.6"></icon>
                                            <icon v-else-if="fileTypeIs(file, 'text')" name="regular/file-alt" scale="2.6"></icon>
                                        </span>
                                    </div>

                                    <div class="__box-info">
                                        {# folder #}
                                        <template v-if="fileTypeIs(file, 'folder')">
                                            <h4>{% verbatim %}{{ file.name }}{% endverbatim %}</h4>
                                        </template>

                                        {# any other #}
                                        <template v-else>
                                            <h4>{% verbatim %}{{ file.name }}{% endverbatim %}</h4>
                                            <small class="__info-file-size" v-if="file.size">{% verbatim %}{{ getFileSize(file.size) }}{% endverbatim %}</small>
                                        </template>
                                    </div>
                                </div>
                            </v-touch>
                        </li>
                    </ul>

                    <infinite-loading v-if="firstRun && files.next"
                                      spinner="waveDots"
                                      @infinite="loadPaginatedFiles">
                        <span slot="no-more"></span>
                    </infinite-loading>
                </v-touch>

            </section>


        </div>

        {# ====================================================================== #}

        {# modals #}
        {% include '@SyliusHappyCMSPlugin/media/partials/modal/ops.html.twig' %}
    </div>
</media-manager>
